#include<stdio.h>
int main()
{
int n,i,count=0;
printf("enter the value");
scanf("%d",&n);
 for(i=1;i<=n;i++)
 {
  if(n%i==0)
    {
       count ++;
    }
}
if(count==2)
{
printf("the number %d is prime",n);
}
else
{
printf(" the number %d is not a prime",n);
}
return 0;
}

